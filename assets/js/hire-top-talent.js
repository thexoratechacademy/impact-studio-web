// Hire Top Talent page specific functionality
document.addEventListener('DOMContentLoaded', () => {
  // Add loaded class to body after a short delay to ensure navbar is rendered
  setTimeout(() => {
    document.body.classList.add('loaded');
  }, 100);
});
